using UnityEngine;

namespace BsseCode.Pools.ShootFires
{
    public class ShootFire : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
